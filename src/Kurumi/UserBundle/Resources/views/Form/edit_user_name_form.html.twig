<form id="{{ form.vars.id }}" novalidate action="{{ path('account_edit_name') }}" {{ form_enctype(form) }} method="post" data-ajax>

    {{ form_errors(form) }}

    {{ form_row(form.name) }}

    {{ form_rest(form) }}

    <div class="form-actions">
        <button type="submit" class="btn btn-primary">Save</button>
        <a class="btn" href="{{ path('account_overview') }}" data-ajax>Cancel</a>
    </div>
</form>