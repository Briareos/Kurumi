{% form_theme form 'MopaBootstrapBundle:Form:fields.html.twig' %}

<form id="{{ form.vars.id }}"
      action="{{ path('edit_user') }}" {{ form_enctype(form) }}
      method="post" data-ajax>

    {{ form_rest(form) }}

    <button type="submit" class="btn btn-primary">Save</button>
</form>