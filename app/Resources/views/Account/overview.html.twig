{% extends ':Account:layout.html.twig' %}

{% block edit_account %}
    <div class="row">
        <div class="span3">
            {% include ':Account:sidebar.html.twig' %}
        </div>
        <div class="span4">

            {% include '::flash_messages.html.twig' %}

            <fieldset>
                <legend>Your account info</legend>
                <dl class="dl-horizontal">
                    <dt>Name</dt>
                    <dd>{{ profile.user.name }} <a href="{{ path('account_edit_name') }}" class="btn btn-mini" data-ajax>Edit</a></dd>
                    <dt>E-mail address</dt>
                    <dd>{{ profile.user.email }} <a href="{{ path('account_edit_email') }}" class="btn btn-mini" data-ajax>Edit</a></dd>
                    <dt>Password</dt>
                    <dd>{% if profile.user.password is empty %}<em>Not set</em>{% else %}**********{% endif %} <a href="{{ path('account_edit_password') }}" class="btn btn-mini" data-ajax>Edit</a></dd>
                    <dt>Timezone</dt>
                    <dd>{% if profile.user.timezone is empty %}<em>Not set</em>{% else %}{{ profile.user.timezone }}{% endif %}</dd>
                    <dt>Language</dt>
                    <dd>{% if profile.user.locale is empty %}<em>Not set</em>{% else %}{{ profile.user.locale }}{% endif %}</dd>
                </dl>
            </fieldset>
        </div>
        <div class="span5">
            <fieldset>
                <legend>Your profile picture <a href="{{ path('account_edit_picture') }}" class="btn btn-mini" data-ajax>Edit</a></legend>
                {% if profile.picture is empty %}
                    <em>Not set</em>
                {% else %}
                    {{ profile_picture(profile, 'profile') }}
                {% endif %}
            </fieldset>
            {% if profile %}
                <fieldset>
                    <legend>Your profile info</legend>
                    <dl class="dl-horizontal">
                        <dt>Gender</dt>
                        <dd>{% if profile.gender %}
                            {% if profile.gender == 1 %}
                                Male
                            {% else %}
                                Female
                            {% endif %}
                        {% else %}
                            <em>Not set</em>
                        {% endif %}</dd>
                        <dt>Birthday</dt>
                        <dd>{% if profile.birthday %}
                            {{ profile.birthday|format_date(null, app.request.locale ) }}
                        {% else %}
                            <em>Not set</em>
                        {% endif %}</dd>
                    </dl>
                </fieldset>
            {% endif %}
        </div>
    </div>
{% endblock %}