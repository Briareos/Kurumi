{% extends 'UserBundle:Profile:page.html.twig' %}

{% block title %}{{ user.name }}{% endblock %}

{% block profile_page %}

    {% include 'PageBundle::flash_messages.html.twig' with {'messages':app.session.flashbag.all()} %}

    {% include 'UserBundle:Profile:tabs.html.twig' %}
    <p>
        {{ profile_info.getSearchInfo(subject.profile) }}
    </p>
{% endblock %}