{% extends ':Account:layout.html.twig' %}

{% block edit_account %}
    <div class="row">
        <div class="span3">
            {% include ':Account:sidebar.html.twig' %}
        </div>
        <div class="span9">

            {% include '::flash_messages.html.twig' %}

            <fieldset>
                <legend>Your picture!
                    {% if profile.picture is empty %}
                        <small>Isn't here =(</small>
                    {% endif %}
                </legend>
                {% include ':Form:profile_picture.html.twig' %}
            </fieldset>
        </div>
    </div>
{% endblock %}