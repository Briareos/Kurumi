<form data-ajax data-modal action="{{ path('picture_comment',{'id':picture.id}) }}" method="post" {{ form_enctype(form) }}>
    {{ form_errors(form) }}

    {{ form_widget(form.body, {'attr':{'class':'input-large'}}) }}

    {{ form_rest(form) }}

    <button type="submit" class="btn btn-primary btn-small">Submit</button>
</form>
